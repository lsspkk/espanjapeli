/**
 * DATA MODEL VERSION - Must be updated with each major application release
 * This version tracks the localStorage data structure format across all stores.
 * When bumping major versions, all storage schemas should be migrated together.
 */
export const DATA_MODEL_VERSION = '4.0.0';

/**
 * Parsed version number for numeric comparisons
 */
export const DATA_MODEL_VERSION_NUMBER = 4;
